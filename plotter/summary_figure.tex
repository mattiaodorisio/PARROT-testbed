\documentclass[varwidth=17cm, border={0.5mm 0.5mm 10mm 0.5mm}]{standalone}
\usepackage{geometry}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{subcaption}
\usepackage{caption}
\usepackage{float}
\usepackage{xcolor}

%% IMPORT-DATA uniform_uint32 test/benchmark_uniform_uint32_20260421_180037.txt
%% IMPORT-DATA normal_uint32 test/benchmark_normal_uint32_20260422_022612.txt
%% IMPORT-DATA mix_gauss_uint32 test/benchmark_mix_gauss_uint32_20260422_105612.txt
%% IMPORT-DATA exponential_uint32 test/benchmark_exponential_uint32_20260422_065752.txt

%% SQL CREATE VIEW uniform_uint32_filtered AS SELECT * FROM uniform_uint32 WHERE (index_name = 'DeLI-Static' AND index_variant = 'GFB;1;70;BI;12;512') OR (index_name = 'DeLI-Dynamic' AND index_variant = 'N;1;70;BI;12;512') OR (index_name = 'DeLI-Static-Payload' AND index_variant = 'GFB;0;70;BI;12;512') OR (index_name = 'DeLI-Dynamic-Payload' AND index_variant = 'N;0;70;BI;12;512') OR (index_name = 'Static-PGM' AND index_variant = '8') OR (index_name = 'ALEX' AND index_variant = 'none') OR (index_name = 'Dynamic-PGM' AND index_variant = '16') OR (index_name = 'SEA21-YFast' AND index_variant = 'sl-bw9') OR (index_name = 'SEA21' AND index_variant = 'hybrid-20') OR (index_name = 'SEA21-Map' AND index_variant = 'hybrid-20') OR index_name NOT IN ('DeLI-Static', 'DeLI-Dynamic', 'DeLI-Static-Payload', 'DeLI-Dynamic-Payload', 'Static-PGM', 'Dynamic-PGM', 'SEA21-YFast', 'SEA21', 'SEA21-Map', 'ALEX')

%% SQL CREATE VIEW normal_uint32_filtered AS SELECT * FROM normal_uint32 WHERE (index_name = 'DeLI-Static' AND index_variant = 'GFB;1;70;BI;12;512') OR (index_name = 'DeLI-Dynamic' AND index_variant = 'N;1;70;BI;12;512') OR (index_name = 'DeLI-Static-Payload' AND index_variant = 'GFB;0;70;BI;12;512') OR (index_name = 'DeLI-Dynamic-Payload' AND index_variant = 'N;0;70;BI;12;512') OR (index_name = 'Static-PGM' AND index_variant = '8') OR (index_name = 'ALEX' AND index_variant = 'none') OR (index_name = 'Dynamic-PGM' AND index_variant = '16') OR (index_name = 'SEA21-YFast' AND index_variant = 'sl-bw9') OR (index_name = 'SEA21' AND index_variant = 'hybrid-20') OR (index_name = 'SEA21-Map' AND index_variant = 'hybrid-20') OR index_name NOT IN ('DeLI-Static', 'DeLI-Dynamic', 'DeLI-Static-Payload', 'DeLI-Dynamic-Payload', 'Static-PGM', 'Dynamic-PGM', 'SEA21-YFast', 'SEA21', 'SEA21-Map', 'ALEX')

%% SQL CREATE VIEW mix_gauss_uint32_filtered AS SELECT * FROM mix_gauss_uint32 WHERE (index_name = 'DeLI-Static' AND index_variant = 'GFB;1;70;BI;12;512') OR (index_name = 'DeLI-Dynamic' AND index_variant = 'N;1;70;BI;12;512') OR (index_name = 'DeLI-Static-Payload' AND index_variant = 'GFB;0;70;BI;12;512') OR (index_name = 'DeLI-Dynamic-Payload' AND index_variant = 'N;0;70;BI;12;512') OR (index_name = 'Static-PGM' AND index_variant = '8') OR (index_name = 'ALEX' AND index_variant = 'none') OR (index_name = 'Dynamic-PGM' AND index_variant = '16') OR (index_name = 'SEA21-YFast' AND index_variant = 'sl-bw9') OR (index_name = 'SEA21' AND index_variant = 'hybrid-20') OR (index_name = 'SEA21-Map' AND index_variant = 'hybrid-20') OR index_name NOT IN ('DeLI-Static', 'DeLI-Dynamic', 'DeLI-Static-Payload', 'DeLI-Dynamic-Payload', 'Static-PGM', 'Dynamic-PGM', 'SEA21-YFast', 'SEA21', 'SEA21-Map', 'ALEX')

%% SQL CREATE VIEW exponential_uint32_filtered AS SELECT * FROM exponential_uint32 WHERE (index_name = 'DeLI-Static' AND index_variant = 'GFB;1;70;BI;12;512') OR (index_name = 'DeLI-Dynamic' AND index_variant = 'N;1;70;BI;12;512') OR (index_name = 'DeLI-Static-Payload' AND index_variant = 'GFB;0;70;BI;12;512') OR (index_name = 'DeLI-Dynamic-Payload' AND index_variant = 'N;0;70;BI;12;512') OR (index_name = 'Static-PGM' AND index_variant = '8') OR (index_name = 'ALEX' AND index_variant = 'none') OR (index_name = 'Dynamic-PGM' AND index_variant = '16') OR (index_name = 'SEA21-YFast' AND index_variant = 'sl-bw9') OR (index_name = 'SEA21' AND index_variant = 'hybrid-20') OR (index_name = 'SEA21-Map' AND index_variant = 'hybrid-20') OR index_name NOT IN ('DeLI-Static', 'DeLI-Dynamic', 'DeLI-Static-Payload', 'DeLI-Dynamic-Payload', 'Static-PGM', 'Dynamic-PGM', 'SEA21-YFast', 'SEA21', 'SEA21-Map', 'ALEX')

\begin{document}

% -----------------------------------------------------------------------
% MULTIPLOT layout: 3 subfigures per row, 4 rows.
%
% Subfigure width formula:
%   gap_total = 0.02 * (cols_per_row - 1) = 0.04
%   subfig_width = (0.95 - 0.04) / 3 = 0.313\textwidth
%
% Columns: LOOKUP_UNIFORM | LOOKUP_IN_DISTRIBUTION | INSERT_DELETE
% Rows:    Uniform | Gaussian | Mix of 2 Gaussians | Exponential
%
% Spacing:
%   - \hspace{0.02\textwidth} after the leftmost subfigure of each row
%   - \par\medskip after each full row except the last
%   - ylabel present only on the leftmost subfigure of each row
% -----------------------------------------------------------------------

\begin{figure}[H]
\centering

% ---- ROW 1: Uniform dataset ----
\par\noindent\textbf{Uniform dataset}\par\smallskip

% Subfigure 1 of 3 — leftmost: keeps ylabel, has column title
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    ylabel={Time (ns)},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.8\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM uniform_uint32_filtered WHERE workload_type = 'LOOKUP_UNIFORM' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup uniform}
\end{subfigure}
%
\hspace{0.02\textwidth}% extra gap after leftmost to compensate for ylabel width
%
% Subfigure 2 of 3
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM uniform_uint32_filtered WHERE workload_type = 'LOOKUP_IN_DISTRIBUTION' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup in-dist.}
\end{subfigure}
%
% Subfigure 3 of 3 — rightmost
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    ymode=log,
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) / CAST(init_num_keys AS REAL) AS y, index_name AS color, index_variant AS marker FROM uniform_uint32_filtered WHERE workload_type = 'INSERT_DELETE' AND CAST(init_num_keys AS INTEGER) >= 1048576 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Insert \& delete}
\end{subfigure}

\par\medskip% row separator

% ---- ROW 2: Gaussian dataset ----
\par\noindent{\color{lightgray}\rule{\linewidth}{0.2pt}}\\[-0.5ex]\textbf{Gaussian dataset}\par\smallskip

% Subfigure 1 of 3 — leftmost: keeps ylabel
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    ylabel={Time (ns)},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM normal_uint32_filtered WHERE workload_type = 'LOOKUP_UNIFORM' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup uniform}
\end{subfigure}
%
\hspace{0.02\textwidth}%
%
% Subfigure 2 of 3
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM normal_uint32_filtered WHERE workload_type = 'LOOKUP_IN_DISTRIBUTION' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup in-dist.}
\end{subfigure}
%
% Subfigure 3 of 3 — rightmost
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    ymode=log,
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) / CAST(init_num_keys AS REAL) AS y, index_name AS color, index_variant AS marker FROM normal_uint32_filtered WHERE workload_type = 'INSERT_DELETE' AND CAST(init_num_keys AS INTEGER) >= 1048576 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Insert \& delete}
\end{subfigure}

\par\medskip% row separator

% ---- ROW 3: Mixture of 2 Gaussians dataset ----
\par\noindent{\color{lightgray}\rule{\linewidth}{0.2pt}}\\[-0.5ex]\textbf{Mixture of 2 Gaussians dataset}\par\smallskip

% Subfigure 1 of 3 — leftmost: keeps ylabel
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    ylabel={Time (ns)},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM mix_gauss_uint32_filtered WHERE workload_type = 'LOOKUP_UNIFORM' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup uniform}
\end{subfigure}
%
\hspace{0.02\textwidth}%
%
% Subfigure 2 of 3
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM mix_gauss_uint32_filtered WHERE workload_type = 'LOOKUP_IN_DISTRIBUTION' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup in-dist.}
\end{subfigure}
%
% Subfigure 3 of 3 — rightmost
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    ymode=log,
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) / CAST(init_num_keys AS REAL) AS y, index_name AS color, index_variant AS marker FROM mix_gauss_uint32_filtered WHERE workload_type = 'INSERT_DELETE' AND CAST(init_num_keys AS INTEGER) >= 1048576 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Insert \& delete}
\end{subfigure}

\par\medskip% row separator

% ---- ROW 4: Exponential dataset ----
\par\noindent{\color{lightgray}\rule{\linewidth}{0.2pt}}\\[-0.5ex]\textbf{Exponential dataset}\par\smallskip

% Subfigure 1 of 3 — leftmost: keeps ylabel
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    ylabel={Time (ns)},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM exponential_uint32_filtered WHERE workload_type = 'LOOKUP_UNIFORM' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup uniform}
\end{subfigure}
%
\hspace{0.02\textwidth}%
%
% Subfigure 2 of 3
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) AS y, index_name AS color, index_variant AS marker FROM exponential_uint32_filtered WHERE workload_type = 'LOOKUP_IN_DISTRIBUTION' AND CAST(init_num_keys AS INTEGER) >= 32768 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Lookup in-dist.}
\end{subfigure}
%
% Subfigure 3 of 3 — rightmost, last row: no \par\medskip after
\begin{subfigure}{0.313\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
    tick label style={font=\normalsize},
    label style={font=\normalsize},
    title style={font=\normalsize},
    xlabel={\# keys},
    xmode=log,
    log basis x={2},
    ymode=log,
    %% ROBUST_Y_MAX
    ymax={10},
    width=\linewidth,
    height=0.80\linewidth,
    grid=major,
    mark=*,
    mark size=0.7pt,
    line width=0.5pt,
]

%% MULTIPLOT SELECT CAST(init_num_keys AS INTEGER) AS x, MEDIAN(CAST(total_time AS REAL)) / CAST(init_num_keys AS REAL) AS y, index_name AS color, index_variant AS marker FROM exponential_uint32_filtered WHERE workload_type = 'INSERT_DELETE' AND CAST(init_num_keys AS INTEGER) >= 1048576 GROUP BY init_num_keys, index_name, index_variant ORDER BY CAST(init_num_keys AS INTEGER)

\end{axis}
\end{tikzpicture}
\captionsetup{justification=centering}
\caption{Insert \& delete}
\end{subfigure}

% -----------------------------------------------------------------------
% SHARED LEGEND — one legend for all subfigures.
% legend columns = min(3, number_of_series)  (capped at 30 entries total)
% Each series needs one \addlegendimage + \addlegendentry pair.
% -----------------------------------------------------------------------
\vspace{0.3em}
\begin{center}
\begin{tikzpicture}
\begin{axis}[
    hide axis,
    xmin=0, xmax=1,
    ymin=0, ymax=1,
    legend columns=5,
    legend style={draw=none, font=\scriptsize},
]

%% SHARED_LEGEND

\end{axis}
\end{tikzpicture}
\end{center}

\label{fig:summary}
\end{figure}

\end{document}
