\documentclass{article}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{geometry}
\geometry{a4paper, margin=1in}

\begin{document}

\title{Benchmark Performance Analysis}
\author{Index Comparison}
\date{\today}
\maketitle

\section{Performance Results}

% Add the plots here
% %% {{PLOT:x_col,y_col,filter,groupby,Title,Caption,Label}}

%% {{PLOT:init_num_keys,MEDIAN(total_time),workload_type=lookup_only,index_name,Time,Lookup Time,index_name}}
%% {{PLOT:init_num_keys,MEDIAN(total_time),workload_type=insert_only,index_name,Time,Insert Time,index_name}}
%% {{PLOT:init_num_keys,MEDIAN(total_time),workload_type=mixed,index_name,Time,Mixed Time,index_name}}

\section{Analysis}

This benchmark compares the performance of different indexing data structures under various workloads. 
The results show the relative performance characteristics of each implementation across multiple metrics:
overall throughput, lookup performance, and insert performance.

\end{document}
